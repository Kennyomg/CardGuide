// import {} from '../actions/cardviewer.js';

const INITIAL_STATE = {
};

const cardviewer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
}

export default cardviewer;
