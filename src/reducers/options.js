// import {} from '../actions/collection.js';

const INITIAL_STATE = {
};

const options = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
}

export default options;
